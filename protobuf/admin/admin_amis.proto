// 定义文件级别的Json标签复制
// @Tag("gorm", "autoUpdateTime:nano")

syntax = "proto3";

package admin;

import "http_config.proto";

option go_package = "github.com/go-home-admin/go-admin/generate/proto/admin";
import "any.proto";

service Amis {
  option (http.RouteGroup) = "admin";

  // 测试
  rpc Demo(DemoRequest)returns(DemoResponse){
    option (http.Get) = "/amis/tabs";
  }
}

message DemoRequest {

}

message DemoResponse {
  string title = 1;
  home.protobuf.Any body  = 2;
}